(define (P x) (+ (x) 10))
(define (O x) + x 10)

(define (X x) + x 10)
(display (X 1))
(newline)

(define (X x) (+ x 10))
(display (X 1))
(newline)

(define (g x) (- x 4))
(display (g 1))
(newline)
(define (f y) (+ y 5))